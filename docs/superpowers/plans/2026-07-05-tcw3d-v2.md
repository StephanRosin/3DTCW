# TCW 3D-Rundgang v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Den 3D-Rundgang der Tennisanlage TC Waidberg realistisch machen: echtes Layout (6 Plätze in einer Reihe), Eingangsportal mit Steinbogen + TCW-Logo + Waidcup-Fahne, Klubhaus mit Pergola-Terrasse und Tribünenstufen, PBR-Texturen, Platz-Details, dichter Wald, Info-Punkte.

**Architecture:** Bestehendes Vanilla-Three.js-Projekt (ES-Module, kein Build-Tool, statisch servierbar) wird modulweise umgebaut. Neue Module `textures.js` (PBR-Loader mit Fallback-Farben), `entrance.js` (Portal), `infopoints.js` (Marker + Overlay). `tennis.js` wird von 3×2-Block auf eine 1×6-Reihe mit gemeinsamer Umzäunung umgestellt. Höhenmodell: Terrasse liegt auf +0,75 m Plateau mit Sitzstufen; `groundHeight(x,z)` liefert die Bodenhöhe für den Spieler.

**Tech Stack:** Three.js (lokal unter `vendor/`), Canvas-Texturen für Banner/Linien, CC0-PBR-Texturen von Poly Haven (lokal unter `assets/textures/`), Playwright-MCP für visuelle Verifikation, `python3 -m http.server` als Dev-Server.

## Global Constraints

- Kein Build-Tool, keine npm-Dependencies; alles läuft als statische Seite über `python3 -m http.server 8123` aus dem Repo-Root `/home/stephan/Dokumente/TCW3D/3DTCW`.
- Nur CC0-Assets (Poly Haven); Texturen max. 1K-Auflösung; Repo-Zuwachs gesamt ≤ 20 MB.
- Fehlende Textur ⇒ Material fällt auf Grundfarbe zurück (kein schwarzes Mesh, keine Exception).
- Desktop only: WASD/Pfeile + Maus (Pointer Lock), Shift = rennen, **E** = Info, ESC = Menü.
- Sprache aller UI-Texte: Deutsch.
- Performance: Bäume ausschliesslich per InstancedMesh; Schattenkarte ≤ 4096²; DrawCalls im Blick behalten (Möbel-Materialien wiederverwenden).
- Logos: `assets/logos/tcw-logo.jpg` (rund, an Eingangsmauer) und `assets/logos/waidcup.png` (Fahne am Eingang) — Quelldateien liegen in `/home/stephan/Dokumente/TCW3D/`.
- Nach jedem Task: Seite lädt ohne Konsolenfehler (Playwright-MCP: `browser_navigate` → `browser_console_messages`), Screenshot als Sichtprüfung, dann `git commit`.
- Git-Identität ist im Repo bereits konfiguriert. Commits lokal, kein Push.

## Verifikations-Rezept (gilt für jeden Task)

1. Server läuft (einmalig starten, im Hintergrund): `python3 -m http.server 8123 --directory /home/stephan/Dokumente/TCW3D/3DTCW`
2. Playwright-MCP: `browser_navigate` → `http://localhost:8123`, 2 s warten.
3. `browser_console_messages`: keine Errors (Warnungen ok).
4. Klick auf „Rundgang starten", dann per `browser_evaluate` teleportieren (`window.__tcw.teleport(x, z, yawGrad)` — ab Task 2 verfügbar) und `browser_take_screenshot` an den im Task genannten Positionen.
5. Screenshot ansehen (Read) und mit dem Soll vergleichen.

## Koordinatensystem / Masterplan

- Plätze-Reihe entlang **X** (Ost–West), Platz-Längsachse entlang **Z** (Nord–Süd). Norden = −Z.
- Platzmitten: `x_i = (i − 2.5) · 15.6` für i = 0…5 (Pitch 15,6 m), alle bei z = 0.
- Gemeinsame Umzäunung: X von −48,8 bis +48,8; Z von −18 bis +18. Höhe 4 m.
- **Klubhaus** (30 × 6 m) nördlich: Mitte bei (−10, −32), Längsseite parallel zur Platzreihe. Steht auf dem Hochplateau (Basis y = 1,5).
- **Hochplateau** (+1,5 m über Platzniveau — USER-VORGABE): ein durchgehendes Band im Norden, Rechteck X −38…75, Z −40…−21. Darauf liegen Klubhaus, Terrasse, der Weg vom Eingang und das Eingangsportal selbst (vom Eingang blickt man auf die Plätze HINUNTER, wie auf Foto TCW6).
  - **Tribünen-/Treppenrampe West** (X −38…8): Sitzstufen über die ganze Südkante der Terrasse, 6 Stufen (je 0,25 m hoch, 0,4 m tief, Gesamttiefe 2,4 m), linearer `groundHeight`-Übergang Z −21…−18,6. Die Treppe hinunter zum Tor zwischen Platz 1 und 2 (x = −31,2) ist Teil dieser Rampe.
  - **Treppe Ost** (X 37…43): gleiche Rampengeometrie, führt zum Tor bei x = +40.
  - Übrige Südkante (X 8…37 und 43…75): Stützmauer (Beton-PBR, 1,5 m hoch, von der Platzseite sichtbar) mit niedriger Hecke obendrauf; Kollisionswand.
- **Nordzaun ohne Windschutz** (USER-VORGABE): Die Zaunseite zum Klubhaus (z = −18) hat KEINE grünen Blachen — nur Maschendraht, damit man von Klubhaus/Terrasse auf die Plätze sieht. Windschutz nur Süd/Ost/West.
- **Eingang** östlich AUF dem Plateau: Steinmauer verläuft N–S bei x = +56 (auf y = 1,5), Bogen-Durchgang bei z = −24; Holzhütte nördlich neben dem Weg (x 58…63, z −32…−26). Spieler-Start aussen östlich: (66, −24) auf dem Plateau, Blick nach Westen.
- **Weg**: gepflasterter Streifen (y = 1,52) vom Bogen westwärts entlang z = −24 bis zur Terrasse, 3 m breit; an der Südkante Blick hinunter auf die Plätze (Hecke/Mäuerchen als Geländer).
- **Wald**-Ring: innerer Radius 75, äusserer 220, ausgespart um den Eingangsbereich (x > 52 && |z + 24| < 8).

---

### Task 1: Assets beschaffen (Logos + PBR-Texturen)

**Files:**
- Create: `assets/logos/tcw-logo.jpg`, `assets/logos/waidcup.png`
- Create: `assets/textures/<set>/…` (siehe Liste)
- Test: Sichtprüfung der Dateien + Grössencheck

**Interfaces:**
- Produces: Dateipfade, die `js/textures.js` (Task 2) als `TEX`-Konstanten referenziert:
  `assets/textures/clay/diff.jpg`, `clay/nor.jpg`, `grass/diff.jpg`, `grass/nor.jpg`,
  `paving/diff.jpg`, `paving/nor.jpg`, `wood/diff.jpg`, `wood/nor.jpg`,
  `stone/diff.jpg`, `stone/nor.jpg`, `concrete/diff.jpg`

- [ ] **Step 1: Logos kopieren**

```bash
cd /home/stephan/Dokumente/TCW3D/3DTCW
mkdir -p assets/logos assets/textures
cp "/home/stephan/Dokumente/TCW3D/TCW Logo.jpg" assets/logos/tcw-logo.jpg
cp "/home/stephan/Dokumente/TCW3D/Waidcup2.png" assets/logos/waidcup.png
```

- [ ] **Step 2: Poly-Haven-Texturen herunterladen (1K JPG, diff + nor)**

Pro Set die Poly-Haven-API nutzen. Kandidaten (ersten verfügbaren nehmen; Verfügbarkeit prüfen mit `curl -s https://api.polyhaven.com/files/<asset> | jq 'has("Diffuse")'`):

| Set | Asset-Kandidaten (Poly Haven IDs) |
|---|---|
| clay | `red_sand`, `red_dirt_01` |
| grass | `leafy_grass`, `grass_medium_01`, `aerial_grass_rock` |
| paving | `concrete_pavers`, `cobblestone_floor_08`, `paving_stones` |
| wood | `weathered_planks`, `wood_planks_dirt`, `worn_planks` |
| stone | `stone_wall_04`, `stone_bricks_02`, `castle_wall_slates` |
| concrete | `concrete_wall_004`, `concrete_wall_001` |

Download-Muster (URL aus API-JSON ziehen; Struktur: `.Diffuse.1k."1k_jpg".url` bzw. bei manchen Assets `.diff_jpg`/`.nor_gl` — JSON zuerst mit `jq keys` inspizieren):

```bash
mkdir -p assets/textures/clay
curl -s https://api.polyhaven.com/files/red_sand -o /tmp/claude-1000/-home-stephan/23c5b8d2-47f9-4163-b4ab-29f470824b5a/scratchpad/ph.json
jq -r '..|.url? // empty' /tmp/claude-1000/-home-stephan/23c5b8d2-47f9-4163-b4ab-29f470824b5a/scratchpad/ph.json | grep '1k' | grep -i -E 'diff|nor' 
# passende URLs (jpg, 1k) mit curl -L nach assets/textures/clay/diff.jpg bzw. nor.jpg laden
```

Falls ein Asset keine `nor`-JPG-Variante hat: nur `diff.jpg` laden — `textures.js` (Task 2) behandelt fehlende Normal-Maps.

- [ ] **Step 3: Grössen prüfen**

```bash
du -sh assets/   # Soll: deutlich unter 20 MB
ls -R assets/
```

- [ ] **Step 4: Commit**

```bash
git add assets && git commit -m "feat: add CC0 PBR textures and club logos"
```

---

### Task 2: Textur-Helfer + Boden/Gras + Teleport-Debughook

**Files:**
- Create: `js/textures.js`
- Modify: `js/world.js` (createGround nutzt PBR-Gras)
- Modify: `js/main.js` (Teleport-Hook)

**Interfaces:**
- Produces (aus `js/textures.js`, von allen späteren Tasks konsumiert):
  - `loadTex(path, {repeat = [1,1], srgb = false}) → THREE.Texture` — lädt asynchron, setzt RepeatWrapping + anisotropy 8; bei Fehler bleibt Textur leer (Material zeigt dann nur `color`).
  - `pbr({dir, color, repeat, roughness = 1, metalness = 0, normalScale = 1}) → THREE.MeshStandardMaterial` — `dir` z.B. `'assets/textures/clay'`; nutzt `diff.jpg` (sRGB) und, falls vorhanden, `nor.jpg`; `color` ist IMMER gesetzt (Fallback, multipliziert die Map leicht getönt → bei Map-Ausfall sichtbare Grundfarbe statt schwarz).
- Produces (aus `js/main.js`): `window.__tcw.teleport(x, z, yawDeg)` — setzt Kameraposition (y via groundHeight, ab Task 6; bis dahin fix 1.7) und Blickrichtung; für Playwright-Screenshots.

- [ ] **Step 1: `js/textures.js` schreiben**

```javascript
import * as THREE from 'three';

const loader = new THREE.TextureLoader();

export function loadTex(path, { repeat = [1, 1], srgb = false } = {}) {
  const tex = loader.load(path, undefined, undefined,
    () => console.warn(`Textur fehlt: ${path} — Fallback-Farbe aktiv`));
  tex.wrapS = tex.wrapT = THREE.RepeatWrapping;
  tex.repeat.set(repeat[0], repeat[1]);
  tex.anisotropy = 8;
  if (srgb) tex.colorSpace = THREE.SRGBColorSpace;
  return tex;
}

/** PBR-Material aus einem Textur-Set-Ordner (diff.jpg [+ nor.jpg]). */
export function pbr({ dir, color = 0xffffff, repeat = [1, 1], roughness = 1, metalness = 0, normalScale = 1 }) {
  const mat = new THREE.MeshStandardMaterial({ color, roughness, metalness });
  mat.map = loadTex(`${dir}/diff.jpg`, { repeat, srgb: true });
  // Normal-Map nur setzen, wenn Datei existiert — Existenz zur Ladezeit unbekannt,
  // deshalb optimistisch setzen; onError entfernt sie wieder.
  const nor = loader.load(`${dir}/nor.jpg`,
    () => { mat.normalMap = nor; mat.normalScale.set(normalScale, normalScale); mat.needsUpdate = true; },
    undefined,
    () => {});
  nor.wrapS = nor.wrapT = THREE.RepeatWrapping;
  nor.repeat.set(repeat[0], repeat[1]);
  return mat;
}
```

- [ ] **Step 2: `world.js` — `createGround` auf PBR-Gras umstellen**

`grassTexture()` löschen, ersetzen durch:

```javascript
import { pbr } from './textures.js';

export function createGround(scene) {
  const size = 800;
  const mat = pbr({ dir: 'assets/textures/grass', color: 0x6a8f4a, repeat: [size / 6, size / 6] });
  const ground = new THREE.Mesh(new THREE.PlaneGeometry(size, size), mat);
  ground.rotation.x = -Math.PI / 2;
  ground.receiveShadow = true;
  scene.add(ground);
  return ground;
}
```

- [ ] **Step 3: Teleport-Hook in `main.js`**

Nach der `window.__tcw`-Zeile:

```javascript
window.__tcw.teleport = (x, z, yawDeg = 0) => {
  camera.position.set(x, 1.7, z);
  camera.rotation.set(0, yawDeg * Math.PI / 180, 0);
  player.setView?.(x, z, yawDeg);   // ab Task 6: hält yaw/pitch des Controllers synchron
};
```

Und in `player.js` dem Rückgabeobjekt eine Methode hinzufügen:

```javascript
setView(x, z, yawDeg) {
  camera.position.x = x; camera.position.z = z;
  yaw = yawDeg * Math.PI / 180; pitch = 0;
},
```

- [ ] **Step 4: Verifikation**

Verifikations-Rezept ausführen. Screenshot: Startposition. Soll: Gras zeigt Fototextur (keine einfarbige Fläche), keine Konsolenfehler.

- [ ] **Step 5: Commit**

```bash
git add js/textures.js js/world.js js/main.js js/player.js
git commit -m "feat: PBR texture helper, photoreal grass, teleport hook"
```

---

### Task 3: Platzreihe 1×6 mit gemeinsamer Umzäunung

**Files:**
- Modify: `js/tennis.js` (Komplettumbau: `buildCourtBlock` → `buildCourtRow`)
- Modify: `js/main.js` (Aufruf + neue Extents)

**Interfaces:**
- Consumes: `pbr` aus Task 2.
- Produces (aus `js/tennis.js`):
  - `export const COURT_PITCH = 15.6, ROW_COURTS = 6;`
  - `export const ENC = { minX: -48.8, maxX: 48.8, minZ: -18, maxZ: 18, h: 4 };`
  - `buildCourtRow(scene) → { courtX: number[] }` — baut 6 Plätze (Sand als EINE grosse Fläche über die ganze Umzäunung, Linien pro Platz, Netz pro Platz), Aussenzaun mit zwei Tor-Lücken auf der Nordseite (bei x = −31,2 und x = +40, je 2,2 m breit), Kollisionsboxen, Flutlichtmasten (5 Stück: Nordzaunlinie x = −15,6/+15,6; Südzaunlinie x = −31,2/0/+31,2; Höhe 14). Nordseite OHNE Windschutz (nur Maschendraht); Süd/Ost/West mit grünem Windschutz.
- Der alte Export `buildCourt`/`FENCE_W`/`FENCE_D` entfällt; `main.js` wird angepasst.

- [ ] **Step 1: `tennis.js` umbauen**

Kernpunkte (Bestandscode für Linien/Netz/Flutlicht wiederverwenden):

```javascript
export const COURT_PITCH = 15.6;
export const ROW_COURTS = 6;
export const ENC = { minX: -48.8, maxX: 48.8, minZ: -18, maxZ: 18, h: 4 };

export function buildCourtRow(scene) {
  const group = new THREE.Group();
  scene.add(group);

  // Eine durchgehende Sandfläche über die ganze Anlage (wie auf den Luftbildern).
  const clay = new THREE.Mesh(
    new THREE.PlaneGeometry(ENC.maxX - ENC.minX, ENC.maxZ - ENC.minZ),
    pbr({ dir: 'assets/textures/clay', color: 0xb0532e, repeat: [24, 9] }));
  clay.rotation.x = -Math.PI / 2;
  clay.position.y = 0.02;
  clay.receiveShadow = true;
  group.add(clay);

  const courtX = [];
  for (let i = 0; i < ROW_COURTS; i++) {
    const cx = (i - (ROW_COURTS - 1) / 2) * COURT_PITCH;
    courtX.push(cx);
    addCourtMarkings(group, cx);   // Linien-Plane + Netz + Netzpfosten (aus Bestandscode)
  }

  buildEnclosureFence(group);      // Aussenzaun 4 m: unten 2.2 m Windschutz, oben Maschendraht
  addFenceColliders();             // Kollision mit 2 Tor-Lücken auf Nordseite (x=-10, x=+40)

  for (const x of [-COURT_PITCH * 2, 0, COURT_PITCH * 2]) {
    buildFloodlight(scene, x, ENC.minZ - 0.8, 14);
    buildFloodlight(scene, x, ENC.maxZ + 0.8, 14);
  }
  return { courtX };
}
```

`addCourtMarkings(group, cx)`: bestehende `lineMaterial()`-Plane bei (cx, 0.035, 0), bestehendes `buildNet` als `addNet(group, cx)` (Netz bei z = 0, entlang X! — Achtung: Platz-Längsachse ist Z, das Netz verläuft quer, also entlang X wie bisher).

`buildEnclosureFence(group)`: wie bisheriges `buildFence`, aber über den ENC-Umfang; Windschutz-Höhe 2,2 m, Farbe 0x2f6d42; Maschendraht darüber als `linkMat` (opacity 0.25); Pfosten alle 4 m; Tor-Lücken NUR in der Kollision, visuell Rahmen aus `metalDark`-Boxen (2,2 m breit, 2,2 m hoch) an beiden Öffnungen.

`addFenceColliders()`:

```javascript
addBox(ENC.minX - 0.1, ENC.minZ - 0.1, ENC.maxX + 0.1, ENC.minZ + 0.1 /*Nordseite*/);
// … Nordseite in 3 Segmente mit Lücken [-11.1,-8.9] und [38.9,41.1]
// Süd-, West-, Ostseite durchgehend.
```

- [ ] **Step 2: `main.js` anpassen**

```javascript
import { buildCourtRow, ENC } from './tennis.js';
const { courtX } = buildCourtRow(scene);
```

Alle Bezüge auf `halfBlockZ` vorerst durch `Math.abs(ENC.minZ)` ersetzen (Terrasse kommt in Task 6 an die richtige Stelle; Zwischenzustand darf provisorisch aussehen, muss aber fehlerfrei laden). Spieler-Start provisorisch: `(0, ENC.minZ - 6)` mit Blick auf `(0, 0)`.

- [ ] **Step 3: Verifikation**

Rezept ausführen. Teleports + Screenshots: `(0, -25, 180)` Blick auf Nordzaun/Plätze; `(0, 0, 90)` auf dem Sand die Reihe entlang. Soll: 6 Plätze nebeneinander, ein Zaun aussenrum, Sand mit Fototextur. Kollisionstest per `browser_evaluate`: Spieler auf `(-10, -20)` teleportieren, vorwärts laufen lassen (W-Taste via `browser_press_key` einige Sekunden) → durch Tor-Lücke auf den Platz gelangen.

- [ ] **Step 4: Commit**

```bash
git add js/tennis.js js/main.js
git commit -m "feat: rebuild courts as single-row 1x6 layout with shared enclosure"
```

---

### Task 4: Windschutz-Banner, Sponsoren, Zaun-Optik

**Files:**
- Modify: `js/tennis.js` (Banner-Generator + Platzierung)

**Interfaces:**
- Consumes: `ENC`, Zaungruppe aus Task 3.
- Produces: `bannerTexture({text, bg, fg}) → THREE.CanvasTexture` (intern); Banner-Planes an Innen- und Aussenseite des Zauns.

- [ ] **Step 1: Banner-Generator + Platzierung**

```javascript
function bannerTexture({ text, bg = '#1c5c34', fg = '#ffffff', sub = '' }) {
  const c = document.createElement('canvas');
  c.width = 1024; c.height = 256;
  const g = c.getContext('2d');
  g.fillStyle = bg; g.fillRect(0, 0, 1024, 256);
  g.strokeStyle = 'rgba(255,255,255,.25)'; g.lineWidth = 6; g.strokeRect(8, 8, 1008, 240);
  g.fillStyle = fg; g.textAlign = 'center'; g.textBaseline = 'middle';
  g.font = 'bold 110px Arial'; g.fillText(text, 512, sub ? 100 : 128);
  if (sub) { g.font = '52px Arial'; g.fillText(sub, 512, 196); }
  const t = new THREE.CanvasTexture(c);
  t.colorSpace = THREE.SRGBColorSpace; t.anisotropy = 8;
  return t;
}
```

Banner-Liste (Plane 6 × 1,5 m, y = 1,2, an der Zaun-Innenseite Nord + Süd verteilt, 0,06 m Abstand vom Windschutz, `side: THREE.FrontSide`, Rotation zur Platzseite):

```javascript
const BANNERS = [
  { text: 'TC WAIDBERG', sub: 'seit 1932', bg: '#26418f' },
  { text: 'WAIDCUP', sub: '18.–26. Juli 2026', bg: '#3d8f3d' },
  { text: 'RACKET SPORT', bg: '#0d7a44' },
  { text: 'GARAGE ZÜRICH-NORD', bg: '#222222', fg: '#e8e0d0' },
  { text: 'VERSICHERUNG WAID', bg: '#7a1f1f' },
  { text: 'SPORTSHOP CITY', bg: '#14506e' },
];
```

Platzierung (Nordseite hat KEINEN Windschutz → dort keine Banner): Südseite innen 6 Banner bei x = −36, −22, −8, 6, 20, 34; West- und Ostseite innen je 1 Banner (Mitte der Seite). Zusätzlich zwei Logo-Banner mit `loadTex('assets/logos/tcw-logo.jpg', {srgb:true})` als quadratische 1,5 × 1,5-m-Planes an der Südseite innen bei x = −43 und +43.

- [ ] **Step 2: Verifikation**

Teleport `(0, -4, -90)` und `(0, 4, 90)`: Banner an beiden Zaunseiten sichtbar, Texte lesbar, TCW-Logo neben dem Tor. Keine Konsolenfehler.

- [ ] **Step 3: Commit**

```bash
git add js/tennis.js
git commit -m "feat: sponsor banners and club logos on court fence"
```

---

### Task 5: Platz-Details (Schiedsrichterstuhl, Schleppnetz, Besen, Bälle, Bänke, rote Schirme)

**Files:**
- Modify: `js/tennis.js` (Detail-Builder, Aufruf in `buildCourtRow`)
- Modify: `js/props.js` (`buildUmbrella` bekommt runde rote Form)

**Interfaces:**
- Consumes: `courtX` aus Task 3, `wood`/`metalDark`-Materialien aus `props.js` (exportieren!).
- Produces: `addCourtDetails(group, cx, index)` — pro Platz: Schleppnetz-Ständer an der Südbande, Linienbesen angelehnt, 2–3 Tennisbälle (Kugeln r=0.033, Farbe 0xd8e63c) verstreut; Bänke + rote Schirme entlang der Nordseite zwischen den Plätzen (Tor-Korridore bleiben frei). KEINE Schiedsrichterstühle (USER-VORGABE, gestrichen).

- [ ] **Step 1: Detail-Builder schreiben**

Schiedsrichterstuhl (Boxen + Zylinder, Sitz auf 1,9 m, Leiter davor, dunkelgrün 0x2e4632); Schleppnetz = Box 2,0 × 0,04 × 0,7 mit `netMaterial()`-artiger Canvas-Textur flach am Boden + Halterahmen; Besen = Zylinderstiel + Bürstenbox, schräg an Zaun gelehnt (rotation.z ≈ 0.25). Bälle mit `MeshStandardMaterial({color: 0xd8e63c, roughness: .6})`. Kollision nur für Schiedsrichterstühle (`addBox(cx+5.9, -0.5, cx+6.5, 0.5)`).

`buildUmbrella` in props.js: Canopy von `ConeGeometry(1.5, 0.55, 8)` auf `SphereGeometry(1.6, 12, 6, 0, Math.PI*2, 0, Math.PI/2.6)` umstellen (runde Schirmform), Standardfarbe Parameter — an den Plätzen 0xc03030 (rot).

Platzierung in `buildCourtRow`: zwischen benachbarten Plätzen an der Nordseite (z = −16): je 1 Bank (`buildBench`, rotY = Math.PI) + je 1 roter Schirm daneben; vor Platz 1 und 6 zusätzlich je 1 Bank an der Südseite.

- [ ] **Step 2: Verifikation**

Teleport `(courtX[0]+8, 3, -120)` (Schiedsrichterstuhl Platz 1 im Bild), `(0, -15.5, 135)` (Bank + roter Schirm + Banner in einem Bild). Konsole sauber.

- [ ] **Step 3: Commit**

```bash
git add js/tennis.js js/props.js
git commit -m "feat: court furniture — umpire chairs, drag nets, brooms, balls, red umbrellas"
```

---

### Task 6: Höhenmodell + Terrassen-Plateau mit Tribünenstufen

**Files:**
- Modify: `js/collision.js` (`groundHeight`)
- Modify: `js/player.js` (Y aus `groundHeight`)
- Modify: `js/props.js` (`buildTerracePlateau`)
- Modify: `js/main.js` (Aufruf)

**Interfaces:**
- Produces (aus `js/collision.js`):
  - `export const PLATEAU = { minX: -38, maxX: 75, minZ: -40, maxZ: -21, h: 1.5, stepDepth: 2.4 };`
  - `export const RAMPS = [ { minX: -38, maxX: 8 }, { minX: 37, maxX: 43 } ];` — X-Bereiche, in denen die Südkante als begehbare Stufenrampe ausgeführt ist (Tribüne West + Treppe Ost).
  - `groundHeight(x, z) → number` — `h` innerhalb des Plateaus; im Streifen `maxZ … maxZ + stepDepth` linearer Übergang auf 0, aber NUR innerhalb eines RAMPS-X-Bereichs; sonst 0.
- Produces (aus `js/props.js`): `buildTerracePlateau(scene)` — Plateau-Optik + Kollision:
  - Deckfläche: Pflaster-PBR-Plane (`assets/textures/paving`) über X −38…8 (Terrassenteil); der Ostteil (X 8…75) bekommt seine Deckfläche als Weg/Rasen in Task 8 (hier nur Beton-Deckstreifen, color 0x9a958c).
  - Südfassade: Beton-Stützmauer (`assets/textures/concrete`, 1,5 m hoch) entlang der ganzen Südkante AUSSER in den RAMPS-Bereichen.
  - Sitzstufen: in beiden RAMPS-Bereichen 6 Stufen-Boxen (je 0,25 m hoch, 0,4 m tief, Holz-PBR `assets/textures/wood`), Oberkanten folgen der Rampe (z = −20.8 … −18.8, Höhen 1.375, 1.125, 0.875, 0.625, 0.375, 0.125).
  - Niedrige Hecke (0,5 m) oben auf der Stützmauer (X 8…37 und 43…75).
  - Kollisionswände: Südkante in den Nicht-Rampen-Segmenten (X 8…37, 43…75) bei z ≈ −21; Westkante (x = −38, Z −40…−21); Nordkante offen lassen (liegt hinter Klubhaus/Wald).
- `js/player.js`: `camera.position.y = EYE_HEIGHT + groundHeight(camera.position.x, camera.position.z);` (sanft: mit `THREE.MathUtils.lerp` Faktor `Math.min(1, 10*dt)` glätten).

- [ ] **Step 1: `groundHeight` implementieren**

```javascript
export const PLATEAU = { minX: -38, maxX: 75, minZ: -40, maxZ: -21, h: 1.5, stepDepth: 2.4 };
export const RAMPS = [ { minX: -38, maxX: 8 }, { minX: 37, maxX: 43 } ];

export function groundHeight(x, z) {
  const p = PLATEAU;
  if (x < p.minX || x > p.maxX) return 0;
  if (z >= p.minZ && z <= p.maxZ) return p.h;
  if (z > p.maxZ && z <= p.maxZ + p.stepDepth
      && RAMPS.some((r) => x >= r.minX && x <= r.maxX)) {
    return p.h * (1 - (z - p.maxZ) / p.stepDepth);
  }
  return 0;
}
```

- [ ] **Step 2: Player-Y + `buildTerracePlateau` + main.js-Aufruf** (Spezifikation im Interfaces-Block)

- [ ] **Step 3: Verifikation**

Teleport `(-10, -16, 0)` Blick nach Norden: Sitzstufen + Plateau + Stützmauer weiter östlich sichtbar. W-Taste: Spieler steigt die Tribüne hinauf (per `browser_evaluate` `__tcw.camera.position.y`: ≈1.7 unten, ≈3.2 oben). Zweiter Test an der Ost-Treppe: Teleport `(40, -16, 0)`, hinauflaufen. Dritter Test: Teleport `(20, -16, 0)` gegen die Stützmauer laufen → blockiert, Y bleibt ≈1.7.

- [ ] **Step 4: Commit**

```bash
git add js/collision.js js/player.js js/props.js js/main.js
git commit -m "feat: terrace plateau with grandstand steps and ground-height model"
```

---

### Task 7: Klubhaus + Pergola-Terrasse realistisch

**Files:**
- Modify: `js/props.js` (`buildClubhouse` Komplettumbau, Pergola mit Bewuchs, Blumenkübel)
- Modify: `js/main.js` (Möblierung auf Plateau-Höhe)

**Interfaces:**
- Consumes: `PLATEAU` aus Task 6, `pbr` aus Task 2.
- Produces: `buildClubhouse(scene) → void` — Gebäude fest verortet: 30 × 6 m, H 3,6 m, Mitte (−10, −32), Basis auf dem Plateau (alle Y-Werte + `PLATEAU.h` = 1,5); dunkle Holzlatten-Fassade (wood-PBR, color 0x4a3826 dunkel getönt), durchgehendes weisses Fensterband (lokal y 1,6…2,6 über Plateauboden) an der Südfassade, flaches überstehendes Dach (0,25 m stark, 0,5 m Überstand, color 0x3c3630), TCW-Logo-Rondell (CircleGeometry r 0,7, tcw-logo.jpg) mittig an der Südfassade; Kollision. Pergola ebenfalls mit `group.position.y = PLATEAU.h`.
- Möbel-Aufstellung (main.js): 4 Tischgruppen + rote Schirme auf dem Plateau (Y-Offset `PLATEAU.h` — `buildTable`/`buildChair`/`buildUmbrella` bekommen optionalen Parameter `y = 0`), 2 Blumenkübel-Reihen (`buildPlanter(scene, x, z, w, d)` — Betonkasten + Hecken-Buschwerk) an der Plateau-Südkante links/rechts der Stufen, Pergola (bestehender Builder, Position Mitte (−10, −27), 24 × 4 m) mit Wein: pro Pfosten eine dunkelgrüne Icosahedron-Kugelgruppe am Kopf + entlang der Querlatten 8–10 abgeflachte grüne Kugeln (`scale.y = 0.4`).

- [ ] **Step 1: Umbau schreiben** (Signaturen oben; Möbel-Builder: `buildTable(scene, x, z, color, y = 0)` etc. — Gruppe `g.position.y = y`; Kollisionsboxen unverändert in XZ)

- [ ] **Step 2: Verifikation**

Teleport `(-10, -14, 0)`: Gesamtblick Klubhaus + Pergola + Terrasse + Stufen. Teleport auf Plateau `(-10, -24, 0)` Blick 180°: Terrasse mit Möbeln, Fensterband, Logo-Rondell. Konsole sauber.

- [ ] **Step 3: Commit**

```bash
git add js/props.js js/main.js
git commit -m "feat: realistic clubhouse with pergola terrace on plateau"
```

---

### Task 8: Eingangsportal (Steinbogen, TCW-Logo, Waidcup-Fahne, Hütte, Weg)

**Files:**
- Create: `js/entrance.js`
- Modify: `js/main.js` (Aufruf, Spieler-Start am Eingang)

**Interfaces:**
- Consumes: `pbr`, `loadTex` (Task 2), `addBox`/`addWall` (collision.js), `wood` (props.js).
- Produces: `buildEntrance(scene) → { startPos: THREE.Vector3, lookTarget: THREE.Vector3 }` — ALLES auf dem Plateau (Basis y = `PLATEAU.h` = 1,5; am einfachsten eine Gruppe mit `group.position.y = 1.5`): Portalmauer bei x = 56 (N–S, Länge 14 m: z −31…−17), Bogen-Durchgang bei z = −24 (Breite 2,6 m, Scheitel 2,9 m über Plateauboden); rundes TCW-Logo (r 0,55) auf der Aussenseite (Ost) rechts neben dem Bogen wie auf TCW6; Fahnenmast (H 6 m) links des Bogens mit Waidcup-Fahne (Plane 1,2 × 1,8, waidcup.png, DoubleSide, statisch); Holzhütte 5 × 6 m mit Pultdach NÖRDLICH des Weges (x 58…63, z −32…−26; Wände wood-PBR, Dach dunkel); Pflasterweg (paving-PBR-Plane, 3 m breit, y = 1,52) von x 66 westwärts entlang z = −24 bis x = 8 (dort beginnt die Terrassen-Pflasterfläche aus Task 6); niedrige Hecke (0,5 m) als Geländer an der Wegsüdkante x 8…37 und 43…56 (auf der Stützmauer aus Task 6, sofern dort nicht schon vorhanden — nicht doppelt bauen); Kollision: Mauer beidseits des Bogens, Hütte, Fahnenmast.
- `startPos = (66, 0, -24)`, `lookTarget = (50, 0, -24)` (Blick durch den Bogen; vom Weg aus sieht man rechts das Klubhaus und links HINUNTER auf die Plätze).

- [ ] **Step 1: Bogen-Geometrie**

```javascript
function archWall() {
  // Mauer als Extrude-Shape mit Bogen-Loch.
  const shape = new THREE.Shape();
  shape.moveTo(-7, 0); shape.lineTo(7, 0); shape.lineTo(7, 3.6); shape.lineTo(-7, 3.6); shape.closePath();
  const hole = new THREE.Path();
  hole.moveTo(-1.3, 0);
  hole.lineTo(-1.3, 1.9);
  hole.absarc(0, 1.9, 1.3, Math.PI, 0, true);   // Halbkreis-Scheitel bei 3.2 → Scheitel 2.9 mit r=1.0? → r=1.3, Scheitel 3.2 ok
  hole.lineTo(1.3, 0);
  hole.closePath();
  shape.holes.push(hole);
  const geo = new THREE.ExtrudeGeometry(shape, { depth: 0.6, bevelEnabled: false });
  return geo; // lokal: X = Mauerlänge (wird zu Welt-Z gedreht), Y = Höhe
}
```

Mesh: `pbr({dir:'assets/textures/stone', color:0x9a938a, repeat:[4,1.2]})`, `rotation.y = Math.PI/2`, Position (56, 0, −24). Kollision: `addBox(55.7, -31, 56.3, -25.4)` und `addBox(55.7, -22.6, 56.3, -17)` (Durchgang z −25.4…−22.6 frei — grosszügiger als der Bogen, Radius des Spielers berücksichtigt).

- [ ] **Step 2: Logo, Fahne, Hütte, Weg, Start** (Spezifikation im Interfaces-Block; Logo-Rondell: `CircleGeometry(0.55, 32)` bei (56.35, 1.9, −21.5), `rotation.y = Math.PI/2` zur Ostseite; Fahne am Mast bei (56.4, oben, −27)).

`main.js`: `const { startPos, lookTarget } = buildEntrance(scene);` ersetzt den provisorischen Start aus Task 3.

- [ ] **Step 3: Verifikation**

Neu laden, „Rundgang starten": Erster Blick muss dem Foto TCW6 (rechts unten) ähneln: Steinmauer mit Bogen, rundes TCW-Logo, Waidcup-Fahne, Hütte rechts. Screenshot. Dann W drücken (Playwright `browser_press_key`) → durch den Bogen laufen, Screenshot von innen (Plätze sichtbar). Kollisionstest: seitlich gegen die Mauer laufen → kein Durchkommen.

- [ ] **Step 4: Commit**

```bash
git add js/entrance.js js/main.js
git commit -m "feat: entrance portal with stone arch, TCW logo, Waidcup flag and hut"
```

---

### Task 9: Info-Punkte (Taste E)

**Files:**
- Create: `js/infopoints.js`
- Modify: `index.html` (#infohint, #infocard + CSS, HUD-Zeile um „E — Info" ergänzen)
- Modify: `js/main.js` (Erzeugung + Update im Loop)

**Interfaces:**
- Consumes: Kameraposition; DOM-Elemente `#infohint`, `#infocard`.
- Produces: `createInfoPoints(scene, camera, defs) → { update(dt) }`;
  `defs: [{ x, z, y = 1.6, title, text }]`.
  Marker: Sprite mit Canvas-„i"-Icon (Kreis 0x26418f, weisses i), sanftes Auf/Ab (±0,1 m, sin). Innerhalb 3 m: `#infohint` sichtbar („E — Info"); Taste E toggelt `#infocard` (Titel + Text des nächsten Punkts); Verlassen des Radius schliesst die Karte.

- [ ] **Step 1: `infopoints.js` + HTML/CSS**

```javascript
import * as THREE from 'three';

export function createInfoPoints(scene, camera, defs) {
  const hint = document.getElementById('infohint');
  const card = document.getElementById('infocard');
  const sprites = defs.map((d) => {
    const c = document.createElement('canvas');
    c.width = c.height = 128;
    const g = c.getContext('2d');
    g.fillStyle = '#26418f'; g.beginPath(); g.arc(64, 64, 56, 0, Math.PI * 2); g.fill();
    g.strokeStyle = '#fff'; g.lineWidth = 6; g.stroke();
    g.fillStyle = '#fff'; g.font = 'bold italic 84px Georgia';
    g.textAlign = 'center'; g.textBaseline = 'middle'; g.fillText('i', 64, 68);
    const tex = new THREE.CanvasTexture(c); tex.colorSpace = THREE.SRGBColorSpace;
    const s = new THREE.Sprite(new THREE.SpriteMaterial({ map: tex, depthTest: true }));
    s.scale.set(0.45, 0.45, 1);
    s.position.set(d.x, d.y ?? 1.6, d.z);
    scene.add(s);
    return { sprite: s, def: d, baseY: d.y ?? 1.6 };
  });

  let open = false, near = null, t = 0;
  window.addEventListener('keydown', (e) => {
    if (e.code !== 'KeyE' || !near) return;
    open = !open;
    if (open) {
      card.querySelector('h2').textContent = near.def.title;
      card.querySelector('p').textContent = near.def.text;
    }
    card.classList.toggle('visible', open);
  });

  function update(dt) {
    t += dt;
    near = null;
    let best = 9;   // = 3m²
    for (const p of sprites) {
      p.sprite.position.y = p.baseY + Math.sin(t * 2 + p.baseY) * 0.1;
      const dx = camera.position.x - p.def.x, dz = camera.position.z - p.def.z;
      const d2 = dx * dx + dz * dz;
      if (d2 < best) { best = d2; near = p; }
    }
    hint.classList.toggle('visible', !!near && !open);
    if (!near && open) { open = false; card.classList.remove('visible'); }
  }
  return { update };
}
```

`index.html` ergänzen (vor `</body>`):

```html
<div id="infohint"><span class="k">E</span> Info</div>
<div id="infocard"><h2></h2><p></p><div class="close">E zum Schliessen</div></div>
```

CSS: `#infohint` unten mittig, Pille, `opacity 0 → .visible {opacity:1}`; `#infocard` rechts mittig, max-width 320px, gleiche Kartenoptik wie `.card`, `transform: translateX(20px)`-Einblendung.

Definitionen in `main.js`:

```javascript
const infoDefs = [
  // Punkte auf dem Plateau liegen 1,5 m höher → y = 1,6 + 1,5 = 3,1.
  { x: 60, z: -22, y: 3.1, title: 'TC Waidberg', text: 'Seit 1932. Tennisclub im Wald auf dem Käferberg, hoch über Zürich — 6 Sandplätze, Klubhaus mit Pergola-Terrasse.' },
  { x: 57, z: -28, y: 3.1, title: 'Waidcup', text: 'Traditionsturnier seit 1959. Nächste Austragung: 18.–26. Juli 2026 beim TC Waidberg Zürich.' },
  { x: -10, z: -22, y: 3.1, title: 'Klubhaus & Terrasse', text: 'Treffpunkt nach dem Match: Pergola-Terrasse mit Blick hinunter auf alle sechs Plätze.' },
  { x: courtX[0] + 4, z: -16, title: 'Platz 1', text: 'Sechs Sandplätze in einer Reihe, gepflegt von Frühling bis Herbst. Mehrere Plätze mit Flutlicht.' },
];
const infoPoints = createInfoPoints(scene, camera, infoDefs);
// im animate(): infoPoints.update(dt);
```

HUD-Zeile in `index.html` ergänzen: `<b>E</b> — Info`.

- [ ] **Step 2: Verifikation**

Teleport `(60, -22, 90)` → Hint „E — Info" erscheint; `browser_press_key` E → Karte mit „TC Waidberg"-Text; nochmal E → zu; weglaufen → Hint verschwindet. Screenshots.

- [ ] **Step 3: Commit**

```bash
git add js/infopoints.js index.html js/main.js
git commit -m "feat: info points with E-key overlay cards"
```

---

### Task 10: Wald-Upgrade + Feinschliff + README + Abnahme

**Files:**
- Modify: `js/props.js` (`buildForest`: 3 Baumtypen, Farbvarianz, Aussparung Eingang)
- Modify: `js/world.js` (Fog/Belichtung feinjustieren), `js/main.js` (Waldparameter)
- Modify: `README.md`

**Interfaces:**
- Consumes: alles Vorherige.
- Produces: finaler Zustand v2.

- [ ] **Step 1: Wald verbessern**

`buildForest(scene, count = 650, innerR = 75, outerR = 220)`: dritter Typ „hohe Fichte" (`ConeGeometry(1.6, 13, 7)`, translate y 7,5); `instanceColor` pro Instanz: Grundfarbe × (0,85…1,15) via `setColorAt`; Aussparung: beim Platzieren `if (x > 52 && Math.abs(z + 24) < 8) continue;` (Slot wird geparkt); Baum-Fusspunkt: `dummy.position.set(x, groundHeight(x, z), z)` (Bäume auf dem Plateau-Rand stehen sonst in der Luft/im Boden); zusätzlich 8–10 Einzelbäume dicht hinter dem Süd- und Westzaun (r 60–70) für den „Wald direkt am Platz"-Eindruck der Fotos.

- [ ] **Step 2: Feinschliff**

- `world.js`: Fog auf `new THREE.Fog(0xcfe3f5, 150, 380)`; `toneMappingExposure = 0.8` (in main.js).
- Schatten: `sun.shadow.mapSize.set(4096, 4096)`, Kamera-Box `s = 120` um die Anlage (reicht: Anlage ~110 m breit).
- Draw-Call-Check per `browser_evaluate`: `__tcw.renderer.info.render.calls` < 300.

- [ ] **Step 3: README aktualisieren** (Layout-Beschrieb 1×6, Steuerung inkl. E, Assets-Ordner + CC0-Hinweis, Startpunkt am Eingang).

- [ ] **Step 4: Abnahme-Rundgang**

Playwright: Start → durch den Bogen → Weg entlang → Terrasse hoch → über die Plätze; an 6 Stationen Screenshots (Eingang aussen, Bogen-Durchblick, Weg mit Blick auf Plätze, Terrasse mit Klubhaus, Platzreihe von Platz 1, Blick von Süd-West-Ecke über alles). Alle Screenshots ansehen: keine Löcher, kein Z-Fighting, Logos korrekt, Konsole fehlerfrei.

- [ ] **Step 5: Commit**

```bash
git add js/props.js js/world.js js/main.js README.md
git commit -m "feat: dense varied forest, lighting polish, updated README"
```

---

## Self-Review (durchgeführt)

- **Spec-Abdeckung:** Layout 1×6 (T3), Zaun+Banner+Logos (T4), Platz-Details (T5), Tribüne (T6), Klubhaus/Terrasse/Pergola (T7), Eingang+Logos+Fahne+Hütte (T8), Info-Punkte (T9), Wald+Licht+Doku (T10), Texturen/Fallback (T1/T2), Kollision & Start am Eingang (T3/T6/T8). Fehlerbehandlung Textur-Fallback: T2. Verifikation: in jedem Task.
- **Platzhalter:** keine „TBD"; repetitive Möbelgeometrie ist mit Massen/Farben/Positionen spezifiziert.
- **Typkonsistenz:** `pbr({dir,…})`, `ENC`, `PLATEAU`, `groundHeight`, `buildCourtRow → {courtX}`, `buildEntrance → {startPos, lookTarget}`, `createInfoPoints → {update}` — überall gleich benannt.
